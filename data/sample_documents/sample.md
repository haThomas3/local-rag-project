# Markdown Sample Document

This markdown file is used to test local document ingestion.

The RAG system should support both plain text files and markdown files.

Markdown support is useful because many technical notes, project documents, and README-style files are written in markdown.
